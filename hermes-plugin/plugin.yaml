name: hermes-vip
description: "Root privilege escalation gateway — LLM never sees approval responses"
version: 1.0.0
author: Hermes Community
license: MIT
platforms: [macos, linux]
hooks:
  - pre_tool_call
